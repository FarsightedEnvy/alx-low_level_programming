#include "main.h"

/**
 * print_alphabet - for this task at hand this is the function
 * Return: each time 0 (success)
 */

void print_alphabet(void)
{
	char k;

	for (k = 'a'; k <= 'z'; k++)
	{
		_putchar(k);
	}
	_putchar('\n');
}
