#include <stdio.h>

/**
 * main - program entry point
 * Return: each time 0 (success)
 */

int main(void)
{
	int k;

	k = 'z';
	while
		(k >= 'a') {
			putchar(k);
			k--;
		}
	putchar('\n');
	return (0);
}
