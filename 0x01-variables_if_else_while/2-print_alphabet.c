#include <stdio.h>

/**
 * main - program entry point
 * Return: each time 0 (success)
 */

int main()
{
	char c;

	c = 'a';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
