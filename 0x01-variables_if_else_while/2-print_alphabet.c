#include <stdio.h>

/**
 * main - program entry point
 * Return: each time 0 (success)
 */

int main(void)
{
	char k;

	k = 'a';
	while
		(k <= 'z') {
			putchar(k);
			k++;
		}
	putchar('\n');
	return (0);
}
