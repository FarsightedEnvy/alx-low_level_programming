#include "main.h"

/**
 * print_numbers - function verifies a digit (0 through 9)
 *
 * Return: each time 0
 */

void print_numbers(void)
{
	int k;

	for (k = 48; k < 58; k++)
	{
		_putchar(k);
	}
	_putchar('\n');
}
