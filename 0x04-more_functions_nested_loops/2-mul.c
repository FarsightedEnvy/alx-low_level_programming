#include "main.h"

/**
 * mul - this function validates a digit (0 through)
 * @a: a - variable
 * @b: b - variable
 * Return: each time 0
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
